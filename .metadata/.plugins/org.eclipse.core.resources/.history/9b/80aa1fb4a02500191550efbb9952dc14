package network;

import java.io.IOException;
import java.net.ServerSocket;
import java.net.Socket;

public class ServerBank {
	private ServerSocket server;

	public ServerBank() {
		try {
			this.server = new ServerSocket(5456);
		} catch (IOException e) {
			e.printStackTrace();
		}
	}
}
